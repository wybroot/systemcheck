# README 预览图说明

当前目录用于存放根 `README.md` 的截图资源。

## 已提供文件

- `dashboard-preview.svg`：Web 仪表盘占位图
- `report-preview.svg`：巡检报告占位图

## 推荐替换方式

将真实截图放到以下文件：

- `dashboard-preview.png`
- `report-preview.png`

然后把根 `README.md` 中的图片链接从 `.svg` 改为 `.png`。
